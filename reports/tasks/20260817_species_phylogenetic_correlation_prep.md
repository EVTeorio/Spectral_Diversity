# Task Report: Species And Phylogenetic Correlation Prep

## Objective

Prepare repository documentation before beginning a focused analysis of correlations between species-diversity and phylogenetic-diversity measures.

## Actions Performed

- Reviewed the core Markdown project context, governance notes, data dictionary, directory map, current project state, variable guides, and recent SV-diversity analysis reports.
- Confirmed that the active analysis-ready inputs are the current combined quadrat tables and the `sv_diversity_analysis_dataset.csv` table.
- Added an execution plan for the species-versus-phylogenetic diversity correlation analysis.
- Updated living project and paper-workspace documentation so the next analysis step is explicit.
- Left `Documents/Paper/SVH_Paper.docx` untouched because it already had pre-existing modifications and no explicit instruction required editing it.

## Prepared Analysis Scope

The next analysis should compare species-diversity measures (`sp_rich`, `sp_shannon`, `sp_simpson`, `sp_even`) with phylogenetic-diversity measures (`phy_faith`, `phy_rao`, `phy_afaith`) within 10 m, 20 m, and 50 m quadrats. It should preserve the current `primary_analysis` and `edge_flag` conventions where those flags are available.

## Notes

This task was documentation-only. No R scripts, CSV outputs, figures, or Word documents were generated or modified.
