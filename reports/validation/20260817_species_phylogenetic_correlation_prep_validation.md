# Validation: Species And Phylogenetic Correlation Prep

## Checks

- Confirmed Markdown files are discoverable with `rg --files -g "*.md"`.
- Reviewed the current project-governance Markdown before editing documentation.
- Confirmed the existing modified Word file `Documents/Paper/SVH_Paper.docx` was present before this task and was not edited.
- Confirmed the current direct SV-diversity report already documents spectral variation versus species/phylogenetic diversity correlations; the upcoming task is distinct because it compares species-diversity measures directly with phylogenetic-diversity measures.

## Result

Documentation is ready for the next analysis step. The planned analysis should start from `sv_diversity_analysis_dataset.csv` or the root-level `quadrat_analysis_*m.csv` files, depending on whether the `primary_analysis` and `edge_flag` columns are needed directly.
